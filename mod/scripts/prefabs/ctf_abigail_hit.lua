---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by darionco.
--- DateTime: 2021-03-10 1:09 a.m.
---

local function fn()
    local inst = CreateEntity();
    
    inst.entity:AddTransform();
    inst.entity:AddAnimState();
    inst.entity:AddNetwork();

    inst:AddTag('FX');
    
    inst.AnimState:SetBank('abigail_debuff_fx');
    inst.AnimState:SetBuild('abigail_debuff_fx');

    inst.AnimState:PlayAnimation('vex_hit');
    inst.AnimState:SetFinalOffset(3);

    if not TheWorld.ismastersim then
        return inst
    end

    inst.persists = false;
    inst:ListenForEvent('animover', inst.Remove);

    return inst
end

return Prefab('ctf_abigail_hit', fn, { Asset('ANIM', 'anim/abigail_debuff_fx.zip') });
